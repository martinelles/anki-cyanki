# Cyanki

O estado do projeto vive em [ESTADO.csv](ESTADO.csv) — mudar status é mudar uma célula.
`VISAO_hoje.md`, `VISAO_quadro.md` e a nota do vault são geradas por
`python scripts/gerar_visoes.py` e nunca editadas à mão; em divergência com qualquer prosa,
o CSV vence.
